import { LoginAndForgotPassword } from './login-and-forgot-password';

describe('LoginAndForgotPassword', () => {
  it('should create an instance', () => {
    expect(new LoginAndForgotPassword()).toBeTruthy();
  });
});
